<h1>Login</h1>

<%= form_for @user, :url => url_for(:action => 'auth') do |f| %>
  <p>
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :password %><br />
    <%= f.password_field :password %>
  </p>
  <p class="button"><%= f.submit %></p>
<% end %>
