
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Movie Trailers</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	 
</head>
<body>
	<div class="navbar navbar-static-top navbar-inverse">
		<div class="navbar-inner">
			<%= link_to 'Home', '/', :class => 'brand' %>
			<ul class="nav">
			  <li class="active">
			    <%= link_to 'Actors', actors_path %>
			  </li>
			  <li><%= link_to 'Movies', movies_path %></li>
			  <li><a href="/setprefs?lang=hi">Hindi</a></li>
			  <li><a href="/setprefs?lang=en">English</a></li>
			</ul>
			<% if @current_user %>
			<%= link_to @current_user + ' Logout', '/logout', :class => 'brand pull-right' %>
			<% else %>
			<%= link_to 'Login', '/login', :class => 'brand pull-right' %>
			<% end %>
		</div>

	</div>
	<div class="jumbotron">
		<div class="container">
			<h1><%=  t :title %></h1>
			<p><%= t('tagline_html') %></p>
			<p><%= l Time.now, format: :short %></p>
			<p><%= User.model_name.human %></p>
			<p><%= User.human_attribute_name("login") %></p>
		</div>
	</div>
	<div class="container">

	<% if notice %>
	<div class="alert alert-danger" role="alert">
	  <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
	  <%= notice %>
	</div>
	<% end %>
    <p><%= @timetaken %></p>
	<%= yield %>
	<% if content_for?(:page_title) %>
    <%= yield(:page_title) %>
    <% end %>
	</div>
	<script type="text/javascript">
  $('select').select2();
</script>
</body>
</html>